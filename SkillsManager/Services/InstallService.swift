// Legacy plugin-bundle installer removed.
//
// Discover now loads directly from https://skills.sh/ and installation uses the
// official `npx skills add ... --skill ...` flow from SkillStore. Local skills
// bundled inside plugin caches remain manageable via filesystem scanning, but
// they are no longer installed through a separate dedicated service path.
